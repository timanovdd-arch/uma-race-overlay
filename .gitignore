# ── Rust build output (генерируется) ──
/target/
**/target/

# ── Проприетарные файлы игры — НИКОГДА не коммитить (копирайт Cygames) ──
GameAssembly.dll
*.mdb
hachimi/
winhttp.dll

# ── Локальный тулчейн / установщики ──
.tools/
*.exe

# ── Готовые сборки → в GitHub Releases, не в репозиторий ──
*.zip
UmaRaceOverlay-RELEASE/
UmaRaceOverlay-KEEP/

# ── Секреты фидбэка (НЕ в публичный репо!) ──
# Клиент: relay_url/relay_key вшивает build.rs. Worker: токен бота + chat_id +
# app-key + ip-salt вводятся через `wrangler secret put`, локальная шпаргалка ниже.
feedback_creds.txt
**/feedback_creds.txt
feedback-relay/secrets.local.txt
**/secrets.local.txt

# ── Геометрия трасс (dev-only, GPLv3-источник uma-skill-tools) ──
# Используется ЛОКАЛЬНО для разработки/валидации accel-тайминга. В MIT-релиз НЕ
# бандлим (конфликт лицензий) — для раздачи генерируем свою копию из игры.
uma-race-overlay-app/data/course_data.json

# ── BMAD-Method (инструмент разработки, не часть мода) ──
_bmad/
_bmad-output/
.claude/skills/

# ── Dev-only заметки и бэкапы (не часть публичного мода) ──
скилы клода/
investigations/
uma-race-overlay-app/design_backup_pre_pastel/

# ── Прочее ──
Thumbs.db
.DS_Store
113541

# Примечание: Cargo.lock НЕ игнорируем — для бинарных крейтов его коммитят
# (воспроизводимость сборки). Ассеты (icon.png, icon.ico, donat.png) в
# uma-race-overlay-app/ тоже коммитятся — они нужны для сборки.
